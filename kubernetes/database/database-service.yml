apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: mern-ecommerce
  labels:
    app: database
spec:
  type: ClusterIP
  selector:
    app: database
  ports:
    - name: mongodb
      port: 27017
      targetPort: mongodb
      protocol: TCP
